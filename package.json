{
  "name": "☀️ GUI Tweaks",
  "version": "0.0.1",
  "type": "module",
  "author": "laktiv",

  "scripts": {
    "sass-build": "sass src/scss/index.scss index.css --no-source-map --color --no-charset",
    "sass-build-plugins": "sass src/scss/plugins/_index.scss plugins.css --no-source-map --color --no-charset",
    "sass-watch": "sass --watch src/scss/index.scss index.css --no-source-map --color",
    "update-npm": "ncu -u"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "fs-extra": "^11.3.2",
    "prettier": "^3.6.2",
    "stylelint": "^16.25.0"
  },
  "devDependencies": {
    "sass": "^1.93.2",
    "stylelint-order": "^7.0.0",
    "stylelint-scss": "^6.12.1"
  }
}